import React from 'react';

const Search = () => {
	return (
		<div>
			For now run the search on this component. If there is time create a search element to include on home page and here to run the search and set the results in state. 
		</div>
	);
};

export default Search;